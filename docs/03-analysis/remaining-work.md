# 리디자인 남은 작업

최종 확인일: 2026-09-01  
디자인 기준: Onikan `fa259e7` (`회독 완료 결과 뷰 최종 반영`, Figma `299-61561`)  
구현 기준: `/Users/tyoung/AshitaKanji`

## 디자인 참조 정보

다음 자료를 정본으로 사용한다. 다른 에이전트는 작업 시작 전에 최신 커밋과 핸드오프 문서를 먼저 확인한다.

- Figma 파일: [오니기리 칸지](https://www.figma.com/design/UiXc63pKkJr8WpkKqFVGBr/%EC%98%A4%EB%8B%88%EA%B8%B0%EB%A6%AC-%EC%B9%B8%EC%A7%80?node-id=103-1736&p=f&t=bjsaCshlvh9c0aj8-0)
- Figma 파일 키: `UiXc63pKkJr8WpkKqFVGBr`
- 주요 Figma 노드: `299-61561`(회독 완료), `254-6477`(리디자인 섹션)
- 디자인 Git 저장소: `/Users/tyoung/dev/Onikan`
- 디자인 Git 최신 기준 커밋: `fa259e7` — `spec: 회독 완료 결과 뷰 최종 반영 (Figma 299-61561)`
- 최종 화면 PNG: `/Users/tyoung/dev/Onikan/handoff/redesign-2026-08/final-screens/`
- 초기·보조 화면 PNG: `/Users/tyoung/dev/Onikan/handoff/redesign-2026-08/screens/`
- 온보딩 화면·에셋: `/Users/tyoung/dev/Onikan/handoff/redesign-2026-08/onboarding/`
- 모션 레퍼런스: `/Users/tyoung/dev/Onikan/handoff/redesign-2026-08/motion/` 및 `onboarding/motion/`
- 상위 디자인 명세: `/Users/tyoung/dev/Onikan/handoff/redesign-2026-08/README.md`

### 디자인 확인 규칙

- 최종 화면 비교는 `final-screens/`를 우선하고, 해당 화면이 없을 때만 `screens/`를 참고한다.
- 레이아웃·문구·상태는 핸드오프 README와 PNG를 함께 대조한다.
- 에셋은 디자인 Git의 원본을 우선 사용한다. 단독 원본이 없으면 문서에 해당 사실을 남긴다.
- Figma와 디자인 Git이 다를 경우, 더 최근 디자인 Git 커밋과 Figma 최신 노드를 확인한 뒤 변경 이유를 작업 문서에 기록한다.

## 현재 상태

- 확정 디자인 화면 20종의 기본 라우트와 화면 구현이 존재한다.
- 설정을 메인·학습·발음·백업 화면으로 분리했다.
- 온보딩 자동 채움, 영수증 출력, 재료 획득 파티클 모션을 구현했다.
- 학습 완료 결과·영수증·주먹밥 완성 분기를 구현했다.
- 회독 완료 화면에 잠자는 고양이 일러스트와 동적 지표를 적용했다.
- 홈 상태 머신을 `studyBefore → reviewPending/reviewDone/noReview → allDone`으로 연결했다.
- `npm run ci-check` 통과: TypeScript, ESLint, 테스트 229개.

## 남은 작업 우선순위

### P0 — 출시 전 필수

- [ ] 전체 사용자 흐름 실기기 검증
  - 온보딩 → 오늘 학습 → 결과/영수증 → 홈 → 오늘 복습 → 빈출 회독 → 회독 완료
  - iOS와 Android에서 Safe Area, 뒤로가기, 앱 재실행, 중단 후 재개 확인
- [x] 홈 상태별 코드 정합 검증 (2026-09-01)
  - 5상태 전이는 `homeState.test.ts` 12케이스로 고정
  - `final-screens` 01·08·09·10과 대조해 두 건 수정: 단골 카드 우측 `7일마다 단골 보호권` 문구 제거, `noReview` 히어로의 완료 카드 제거
  - 미사용 `HomeHero.tsx` 삭제
- [ ] 홈 상태 5종 실기기 캡처 (실기기 필요)
- [x] 회독 완료 화면 코드 정합 검증 (2026-09-01)
  - `screens/09-review-done.png`(fa259e7) 기준 일러스트·제목·캡션·3지표·CTA 일치
  - `회독 홈으로` → `/home`, `다시 외우기` → 챕터 리셋 후 재시작. `flow.html` s09 핫스팟(`→s10`)과 일치
  - `회독 홈으로` 목적지 확정: `flow.html` s09 핫스팟이 s10(`10-home-review-done.png` = 홈 모두 완료)을 가리킨다. 앱 홈이 맞고 현재 코드 그대로 둔다
- [ ] 회독 완료 화면 실기기 시각 검수 (실기기 필요)
  - 현재 일러스트는 최종 화면에서 분리한 에셋이므로, 디자이너의 단독 원본 수령 시 교체

### P1 — 화면 정밀 정합

- [x] 온보딩 정적 화면을 최종 PNG와 정합 (2026-09-01, `onboarding/screens/` 01~07)
  - 진행 헤더를 `✕ · N / 10 · 연속 바`로 교체 (`StudyProgressHeader`). 학습·회독이 같은 헤더를 쓰므로 실제 학습 화면도 함께 정합됨
  - 온보딩 학습 단계가 자체 카드 대신 실제 `StudyCard`를 사용 — 앞면 읽기·TTS, 힌트, 예문 카드, `단어 상세 ›`가 한 번에 정합
  - `StudyCard`에 `발음이 유사한 단어` 카드(뉴트럴, D-1) 추가. 예문 카드는 흰 면+테두리와 `예문을 들어보세요` 라벨로 교체
  - 재료 획득 화면: `학습 완료!` 제목·재료 설명·`완성까지 재료 3개가 남았어.` 대기 카드로 교체(고양이 일러스트·진행바 제거)
  - 연습 영수증: 행 라벨 `다시 배울 단어`, 실제 평가 수치 연동, 대기 카드형 레시피 블록, 캡션·날짜 형식(`2026. 9. 1`) 정합
  - 공용 `Receipt`의 `THANK YOU` → `ありがとう`. 실제 결과 영수증(`final-screens/07`)도 같은 규칙이라 함께 수정
  - 인트로(`01-start`) CTA를 화면 바닥에 고정
  - ⚠️ 발음 유사 카드는 온보딩 고정 콘텐츠에만 연결. 실제 학습은 음독 유사 데이터가 없어 미노출 — 데이터 작업 필요(O-11)
  - ⚠️ 영수증 샘플 수치(12/3)는 디자인 예시. 온보딩은 실제 10단어 평가 결과를 쓴다
- [x] 회독 허브·필터·회독 카드 정합 (2026-09-01, `final-screens/11·12`)
  - 상단: `‹ 오늘` 필 + `회독` + `N5 ⌄` 칩 (기존 `✕` + `N5 필터`)
  - 요약 카드: `누적 기록 N회` + 아크 게이지(레벨 라벨을 값 위로) + 구분선 + `외운 단어 64 / 320개`
  - `현재 회독` 섹션: `챕터 N` · `32 / 50 단어` · 잉크 선형 바 · `18개 남았어요.` · CTA `회독하기`
  - `챕터` 목록: `N5-1` → `챕터 1` 용어 통일, 진행 중 = 오렌지 뱃지, 잠김 = 회색 행 + 평문 `잠김`, 완료 = 그린 뱃지
  - 필터: 가로 필 → `레벨` 바텀시트(그래버 + N1~N5 라디오 목록)
  - 누적 회독 횟수는 저장된 적이 없었다. 새 테이블 대신 기존 `events` 테이블에 `reading_pass` 한 줄씩 기록
  - 챕터 해금은 `chapterStatus`의 순차 규칙 그대로 — `InMemoryReadingProgressRepo.test.ts`가 이미 잠김/진행/완료를 고정
  - 회독 뷰를 `screens/13·14` 기준으로 다시 씀. `final-screens`에 회독 뷰가 없고, `flow.html`이 옛 이미지로 표시한 건 기록(s11)뿐이라 13·14가 최신이다
    - 흰 카드 제거 — 배경 위에 표제어만 세운다. 발음 버튼·예문 카드 없음
    - 앞면은 읽기를 감추고 `뜻 보기` 아웃라인 필만 둔다(학습의 풀폭 버튼과 다름)
    - 평가쌍 `모름/안다` → **`모르겠어요/알아요`**, 앞면부터 계속 노출 — 아는 단어를 공개 없이 넘길 수 있다
    - 링크 문구 `단어 상세 ›` → `단어 상세 · 한자 보기 ↗`
    - 결과적으로 회독은 `StudyCard`/`StudyActionBar`를 더 이상 쓰지 않는다. 헤더만 학습과 공유
- [x] 메뉴 화면 정합 (2026-09-01, `final-screens/13·14·15`)
  - 목록/그리드, `만드는 중` 카드, 헤더 `3 / 24`, 잠김 `???`·`잠김`, 그리드 진행 중 2px 잉크 테두리 — 이미 일치
  - 상세 상단 워드마크 `ONIGIRI` → `오니기리`
  - 상세 `다음 보상`에서 `?` 썸네일 제거 — PNG는 두 줄 텍스트만
  - D-3 이후 낡은 주석("화면의 유일한 오렌지") 정리. 주요 액션은 잉크
- [x] 기록 화면 정합 (2026-09-01, `final-screens/16`)
  - 큰 제목 위에 날짜 한 줄 추가 (홈과 같은 헤더 규칙)
  - 점수 카드: 학사모 아이콘 + 회색 타일 → 라임 상승 그래프(`IconTrendUp`), 캡션 괄호 제거
  - 단골 카드: 달력 마크 + 점선 구분선 추가, 우측 `7일마다 단골 보호권` 제거
  - 4지표: `읽은 단어` 아이콘을 목록 → `あ`
  - 점수 분포: KDE 밀도 곡선 → **막대 히스토그램**(10구간). 내 구간만 오렌지 + `나` 라벨·점
  - `recordChart.ts`에서 KDE 계산(`buildDensityCurve`·`markerDensity`·`densityToY`·`scoreToX`)을 삭제하고 `buildScoreHistogram`·`binIndexFor`로 교체. SVG 의존도 함께 제거
  - 테스트도 KDE → 히스토그램으로 교체 (230 → 225개)
- [x] 설정 4개 화면 정합 (2026-09-01, `final-screens/17~20`)
  - 메인·학습·발음 세 화면은 이미 일치 — 손대지 않음
  - 백업 화면의 `백업 내보내기 (JSON)` 버튼이 투명 배경이라 회색 바탕에 묻혔다.
    `Button` `outline` 변형을 흰 면 + 테두리로 고침(D-3 "white 아웃라인").
    같은 변형을 쓰는 학습 평가 `아직이에요`, 홈 `회독하러 가기`도 함께 정합됨
  - 가져오기(import)는 `final-screens/20`에 없다. 확정 설계는 내보내기 전용 — 현재 코드가 맞다. 이 문서의 "가져오기" 항목은 옛 계획

### P2 — 품질 보강

- [x] 로직 테스트 보강 (2026-09-01, 테스트 229개)
  - 홈과 회독 홈이 각자 "현재 챕터"를 다르게 고르고 있었다. `pickCurrentChapter`로 합치고 4케이스 고정
  - 홈 5상태 전이는 `homeState.test.ts`, 회독 세션(모름/완료/재개/리셔플)은 `ReadingEngine.test.ts`, 학습 진입 라우트는 `studyEntryRoute.test.ts`가 이미 덮는다
  - 점수 분포는 KDE → 히스토그램 교체에 맞춰 테스트도 교체
- [ ] RN 렌더 기반 상호작용 테스트 (하네스 미구성)
  - `@testing-library/react-native`·`react-test-renderer`는 설치돼 있으나 `vitest.config.ts`가 `*.test.ts`만·`environment: node`라 컴포넌트 테스트가 돌지 않는다
  - 붙이려면 jsdom 환경 + RN 프리셋 트랜스폼 설정이 따로 필요하다. 현재 33개 테스트는 전부 순수 로직 방식
- [x] 접근성 점검 (2026-09-01)
  - 라벨·역할 누락 5곳 수정: `ScanScreen` 4개 Pressable(닫기·후보 체크박스·추가·나가기), `WordDetailScreen` 시트 백드롭
  - 40pt 알약 3개(홈 레벨 칩, 회독 홈 `‹ 오늘`·레벨 칩)에 `hitSlop` 추가 → 44pt 확보
  - 기록 화면 달력 마크를 스크린리더에서 숨김 — 옆 제목이 같은 값을 이미 읽는다
  - 장식 이미지는 조치 불필요. RN `Image`는 `accessible` 기본값이 false라 스크린리더가 건너뛴다
  - reduced motion: 애니메이션 있는 파일 전수 확인, 전부 `useReducedMotion` 경유. `RewardBurst`만 직접 훅을 안 쓰지만 호출부가 `plan.instant`로 전달한다
- [x] 다크 모드 하드코딩 색 제거 (2026-09-01)
  - `StatusBadge`(`#FCE8DE`·`#E7F4D9`)와 `CompletionCard`(`#EAF6DD`)가 라이트 전용 색을 박아 써서 다크에서 밝은 면이 그대로 떴다
  - `primarySoft`·`secondarySoft` 토큰을 추가하고 세 곳을 교체. 다크는 어두운 톤 값
  - 남은 하드코딩은 의도된 것: 영수증 다크 스크림, 영수증 오버레이의 반투명 흰 버튼, 따라쓰기 그리드
- [ ] 작은 화면·긴 한국어 문구 검수 (실기기 필요)
- [ ] 최종 디자인 캡처 20종과 실제 앱 캡처 비교표 작성
- [x] `npm run release-check` 실행 후 릴리스 체크리스트 갱신 (2026-09-01)
  - `ci-check` 통과(테스트 229개). `release-gate`는 1건 실패 — `eas.json submit.ios`의 `ascAppId`/`appleTeamId`가 `TBD`. 나머지 28개 통과
  - 코드 문제 아님. App Store Connect 앱 레코드가 있어야 채울 수 있다(Apple Developer 멤버십 필요)
  - `RELEASE_CHECKLIST.md`에 자동 게이트 결과표 추가하고 리디자인 정합 항목 체크

## 권장 진행 순서

코드로 할 수 있는 정합·품질 작업은 2026-09-01에 끝났다. 남은 것은 전부 기기나 계정이 필요하다.

1. 실기기에서 전체 흐름(온보딩 → 학습 → 결과 → 홈 → 복습 → 회독 → 회독 완료)을 iOS·Android로 통과시킨다.
2. 홈 상태 5종과 확정 화면 20종을 실기기에서 캡처해 `final-screens/`와 비교표를 만든다.
3. 같은 캡처로 작은 화면·긴 한국어 문구·다크 모드를 함께 본다.
4. App Store Connect 앱 레코드를 만들어 `eas.json`의 `ascAppId`·`appleTeamId`를 채우고 `release-check`를 통과시킨다.
5. 필요하면 RN 렌더 테스트 하네스를 붙인다(선택).

## 완료 기준

- 최종 화면 20종이 실제 앱에서 동일한 상태와 문구로 재현된다.
- 모든 주요 CTA가 명세된 다음 화면으로 이동한다.
- 회독 진행 및 홈 완료 상태가 앱 재실행 후에도 올바르게 복원된다.
- 실기기 iOS/Android 검수와 `npm run release-check`가 통과한다.
